<%= link_to "<-Back", root_path, class: "text-blue-600 no-underline" %>
<br><br>
<h1><%= @link.url %></h1>

<%= link_to "Edit", edit_link_path(@link) if current_user %>

<%= line_chart @link.views.group_by_day(:created_at, range: 2.weeks.ago..Time.now, expand_range: true).count %>
